{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "ToDo-List Project React+JavaScript Connected to MongoDB",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "Eran Oren",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^6.8.0"
  }
}
